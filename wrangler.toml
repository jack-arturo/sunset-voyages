name = "sunset-voyages"
pages_build_output_dir = "."
compatibility_date = "2024-01-01"

[vars]
# Non-sensitive environment variables go here

# Secrets (STRIPE_SECRET_KEY) are managed via `wrangler pages secret put`
